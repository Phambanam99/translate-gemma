# TranslateGemma Docker Environment Variables
# Used by docker-compose.yml

# GPU Configuration – GPU 2 is mapped as device 0 inside container
NVIDIA_VISIBLE_DEVICES=2
CUDA_VISIBLE_DEVICES=0
GPU_DEVICE_ID=0

# Port (single entry point via nginx)
APP_PORT=8028

# Optional: Hugging Face Token (for gated models)
