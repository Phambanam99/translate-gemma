# ============================================================================
# TranslateGemma Frontend Environment Configuration
# ============================================================================

# Backend API URL (used in development)
# In production with Docker, the frontend is served from nginx which proxies to the backend
# VITE_API_URL defaults to {window.location.origin}/api if not set
# VITE_API_URL=http://localhost:8000/api

# Environment (development, staging, production)
VITE_ENVIRONMENT=production

# Application name
VITE_APP_NAME=TranslateGemma

# Enable analytics (optional)
# VITE_ENABLE_ANALYTICS=false
